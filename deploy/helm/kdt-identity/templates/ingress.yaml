{{- if .Values.ingress.enabled }}
{{/*
  Le portail reçoit des mots de passe et distribue des credentials : il n'a rien à faire en
  clair. Le cookie de session porte l'attribut `Secure`, donc un navigateur ne le renverra
  même pas sur du HTTP — le portail serait inutilisable, silencieusement.
*/}}
{{- if not .Values.ingress.tls.enabled }}
{{- fail "ingress.tls.enabled=false : le portail transporte des mots de passe et des clés privées, et son cookie de session est marqué Secure. Le servir en clair le rend inutilisable autant que dangereux." }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "kdt-identity.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "kdt-identity.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  tls:
    - hosts:
        - {{ required "ingress.host est obligatoire quand l'ingress est activé" .Values.ingress.host | quote }}
      secretName: {{ .Values.ingress.tls.secretName | default (printf "%s-tls" (include "kdt-identity.fullname" .)) }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "kdt-identity.fullname" . }}
                port:
                  name: http
{{- end }}
